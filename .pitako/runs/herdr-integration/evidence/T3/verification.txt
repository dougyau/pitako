Command: bun test tests/herdr.test.ts tests/agent.test.ts tests/profile.test.ts
Result: 44 pass, 0 fail.
tsc --noEmit: clean.

agent_supervise uses an injected herdr runner. Tests do not split a pane.
Missing presence, a non-current Pi integration, and an incompatible server fail before split.
The missing-integration error names `herdr integration install pi`.
Happy-path argv includes --current, --no-focus, both env vars, --kind pi, --no-approve, and --exclude-tools agent_run,agent_supervise. It does not include --approve.
Start timeout and agent_not_ready do not prompt and do not close the pane.
Prompt outcomes do not close the pane or resubmit.
Start failure before a live agent closes only the created pane.
Parent abort sends ctrl+c and then closes that pane.
A second overlapping call fails without a queue.
extensions/agent/run.ts has no herdr argv.
childActiveTools drops agent_supervise.

Live pane dogfood was not run. herdr integration status still shows pi not installed. The plan forbids installing it from Pitako.
